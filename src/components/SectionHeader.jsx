import React from 'react'

const SectionHeader = ({ title }) => {
  return (
    <div className="text-center text-4xl font-serif font-semibold italic underline">{title}</div>
  )
}

export default SectionHeader